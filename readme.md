# LEER DATOS DE BOLSA

Este proyecto intenta obtener las cotizaciones de una serie de valores y pintarlas en una grafica de R.

Los datos son obtenidos principalmente de Yahoo finance, y se descargan y grafican en el entorno de R.

# Paquetes necesarios
Es necesario tener instaladas una serie de librerías que se detallan a continuación.

Seguramente la primera vez que ejecutemos el script nos pida instalar una serie de librerías o paquetes, para ello usaremos el comando install.packages():

    # install.packages("nombredelpaquete");
    # install.packages("tseries");
    # install.packages("timeSeries");
    # install.packages("timeDate");
    # install.packages("dygraphs");
    # install.packages("zoo");
